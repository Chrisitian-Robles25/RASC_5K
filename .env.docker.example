# ============================================================================
# Server5K - Variables de Entorno para Docker
# ============================================================================
# INSTRUCCIONES:
#   1. Copia este archivo a .env: cp .env.example .env
#   2. Genera una SECRET_KEY segura (ver comando abajo)
#   3. Configura ALLOWED_HOSTS con tu dominio o IP
#   4. Ejecuta: docker compose up -d
# ============================================================================

# =============== SEGURIDAD =================
# IMPORTANTE: Genera una clave secreta con:
# python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
SECRET_KEY=CAMBIA_ESTO_genera_una_clave_secreta_de_50_caracteres

# DEBUG: False para producción
DEBUG=False

# ALLOWED_HOSTS: Dominios/IPs permitidos (separados por coma)
# Ejemplos: midominio.com,api.midominio.com,192.168.1.100
ALLOWED_HOSTS=localhost,127.0.0.1

# =============== PUERTOS =================
# Puerto donde estará disponible la aplicación web
WEB_PORT=8000

# Puerto de PostgreSQL (externo, para conectar desde tu máquina)
POSTGRES_PORT=5432

# Puerto de Redis (externo, para conectar desde tu máquina)
REDIS_PORT=6379

# =============== BASE DE DATOS =================
POSTGRES_DB=server5k
POSTGRES_USER=server5k
POSTGRES_PASSWORD=CAMBIA_ESTO_password_seguro_para_postgres

# Host interno de Docker (NO CAMBIAR)
POSTGRES_HOST=postgres

# SSL Mode: 'disable' para Docker local, 'require' para producción con SSL
POSTGRES_SSLMODE=disable

# =============== REDIS =================
# Host interno de Docker (NO CAMBIAR)
REDIS_HOST=redis

# =============== CORS =================
# Orígenes permitidos para peticiones desde el frontend
# Ej: https://app.midominio.com,https://admin.midominio.com
CORS_ALLOWED_ORIGINS=http://localhost:3000,http://127.0.0.1:3000

# =============== EMAIL (OPCIONAL) =================
# Descomenta y configura si necesitas enviar emails
# EMAIL_HOST=smtp.gmail.com
# EMAIL_PORT=587
# EMAIL_USE_TLS=True
# EMAIL_HOST_USER=tu_email@gmail.com
# EMAIL_HOST_PASSWORD=tu_password_de_app
# DEFAULT_FROM_EMAIL=noreply@midominio.com
